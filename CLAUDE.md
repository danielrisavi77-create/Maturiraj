# Maturiraj Claude worker

Follow `AGENTS.md` and `docs/agent-workflow/RULES.md`.
When dispatched by the controller, implement only the provided task and paths. Do not start agents, commit, push, merge or deploy. The controller runs verification and owns release decisions. Return concise structured evidence and report blockers without bypassing permissions or subscription limits.
